# INDEX for the AI .pt checkpoints&models

The current state is the following:

* 5_5/. have the v5.5 full 19683 ternary operations 100% coverage BUT it enforces non-euclidean behaviour through euclidean algorithms, so the mathematical purity is lost, BUT it could be used as a "bridge" system between both non-euclidean and euclidean, as well as "continuum and discrete bridge" because of the underlying implication of what 100% coverage means for a simple VAEs+Controller system.

* 5_11/v5_11_homeostasis/. contains the most mature version of the trained models