# P-Adic VAE V7.2 - Requirements
# Python 3.10+

# Core Dependencies
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Hyperbolic Geometry
geoopt>=0.5.0  # Riemannian optimization and manifold operations

# Configuration
pyyaml>=6.0

# Machine Learning (used in training loop for live ARI computation)
scikit-learn>=1.3.0  # KMeans, adjusted_rand_score

# Training Visualization (REQUIRED — without this, all TensorBoard logging is silently skipped)
tensorboard>=2.13.0

# Progress Bars
tqdm>=4.65.0

# System Monitoring
psutil>=5.9.0  # RAM/GPU monitoring for HardwareMonitor

# Visualization (used by diagnose_direction_geometry.py and analysis scripts)
matplotlib>=3.7.0
umap-learn>=0.5.0  # UMAP dimensionality reduction for direction space analysis
pillow>=10.0.0     # Image backend for matplotlib

# Data Analysis
pandas>=2.0.0

# Testing
pytest>=7.3.0
pytest-cov>=4.1.0
